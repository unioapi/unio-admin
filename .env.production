# 生产构建配置。本文件会进版本库，只放非机密的部署参数，绝不要写 token 或密码。
# 需要机密时用 .env.production.local（被 *.local 规则忽略）或 CI 环境变量，两者都优先于本文件。
#
# VITE_ADMIN_API_BASE：Admin API 基址，构建期被静态烘进产物。
#   - 留空表示同源相对路径，由反向代理把 /admin/v1 转给 admin-server（推荐：无跨源、协议跟随页面）
#   - 或填完整地址，必须是 https 且非本机地址，例如 https://admin.example.com
#
# 构建期会校验该值：缺失、非 https 或指向 localhost 都会让 `bun run build` 直接失败。
VITE_ADMIN_API_BASE=
